name: Build and Test

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  test:
    name: Test Compilation
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: 'frontend/pnpm-lock.yaml'

      - name: Setup Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: pnpm install

      - name: Build frontend
        working-directory: ./frontend
        run: pnpm run build

      - name: Build application (test)
        run: go build -o lunabox-test.exe .

    #   - name: Run Go tests
    #     run: go test ./... -v

  # build-dev:
  #   name: Build Development Version
  #   runs-on: windows-latest
  #   needs: test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Go
  #       uses: actions/setup-go@v5
  #       with:
  #         go-version: '1.23'

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'

  #     - name: Install pnpm
  #       uses: pnpm/action-setup@v4
  #       with:
  #         version: 9

  #     - name: Setup Wails
  #       run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

  #     - name: Install frontend dependencies
  #       working-directory: ./frontend
  #       run: pnpm install

  #     - name: Build with Wails (dev tag)
  #       run: wails build -devtools

  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: lunabox-dev-windows-amd64
  #         path: build/bin/LunaBox.exe
